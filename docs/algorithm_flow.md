# 核心度量模块算法流程图

## 1. AST解析流程

```
┌─────────────────┐
│ 读取Java文件    │
└────────┬────────┘
         │
┌────────▼────────┐
│ 调用plyj解析器  │
└────────┬────────┘
         │
┌────────▼────────┐
│ 生成抽象语法树  │
└────────┬────────┘
         │
┌────────▼────────┐
│ 提取结构信息    │
└────────┬────────┘
         │
┌────────▼────────┐
│ 返回结构字典    │
└─────────────────┘
```

## 2. LoC统计流程

```
┌─────────────────┐
│ 读取Java文件    │
└────────┬────────┘
         │
┌────────▼────────┐
│ 逐行分析        │
└────────┬────────┘
         │
┌────────▼────────┐
│ 统计空行        │
└────────┬────────┘
         │
┌────────▼────────┐
│ 统计注释行      │
└────────┬────────┘
         │
┌────────▼────────┐
│ 统计代码行      │
└────────┬────────┘
         │
┌────────▼────────┐
│ 返回统计结果    │
└─────────────────┘
```

## 3. 圈复杂度计算流程

```
┌─────────────────┐
│ 提取方法体      │
└────────┬────────┘
         │
┌────────▼────────┐
│ 清理代码        │
└────────┬────────┘
         │
┌────────▼────────┐
│ 统计控制结构    │
└────────┬────────┘
         │
┌────────▼────────┐
│ 计算复杂度      │
└────────┬────────┘
         │
┌────────▼────────┐
│ 返回复杂度值    │
└─────────────────┘
```

## 4. CK指标计算流程

### WMC (加权方法复杂度)
```
┌─────────────────┐
│ 获取类方法列表  │
└────────┬────────┘
         │
┌────────▼────────┐
│ 计算每个方法复杂度 │
└────────┬────────┘
         │
┌────────▼────────┐
│ 求和得到WMC     │
└─────────────────┘
```

### CBO (对象间耦合度)
```
┌─────────────────┐
│ 分析类依赖      │
└────────┬────────┘
         │
┌────────▼────────┐
│ 检查父类        │
└────────┬────────┘
         │
┌────────▼────────┐
│ 检查接口        │
└────────┬────────┘
         │
┌────────▼────────┐
│ 检查字段类型    │
└────────┬────────┘
         │
┌────────▼────────┐
│ 检查方法参数    │
└────────┬────────┘
         │
┌────────▼────────┐
│ 检查方法调用    │
└────────┬────────┘
         │
┌────────▼────────┐
│ 去重计数        │
└─────────────────┘
```

### RFC (响应集合)
```
┌─────────────────┐
│ 统计类方法数    │
└────────┬────────┘
         │
┌────────▼────────┐
│ 统计方法调用数  │
└────────┬────────┘
         │
┌────────▼────────┐
│ 求和得到RFC     │
└─────────────────┘
```

### LCOM (类内聚缺失程度)
```
┌─────────────────┐
│ 提取字段和方法  │
└────────┬────────┘
         │
┌────────▼────────┐
│ 分析方法字段使用 │
└────────┬────────┘
         │
┌────────▼────────┐
│ 计算共享与非共享 │
└────────┬────────┘
         │
┌────────▼────────┐
│ 计算LCOM值      │
└─────────────────┘
```